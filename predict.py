import csv

from node import Node
from tree import Tree

def main():
	
	while (
	print ('Informe as instancias: (Digite . para terminar)')


def predict(filename, tree):
	
	attributes, instances = getDataFromFile(file_name)
	
	tree = 
